[phases.setup]
nixPkgs = ["nodejs", "yarn"]

[phases.install]
cmds = ["yarn install"]

[phases.build]
cmds = [
    "cd apps/backend",
    "yarn generate:oas",
    "cd ../..",
    "yarn codegen",
    "yarn build",
]

[start]
cmd = "yarn start"

[variables]
NODE_ENV = "production"
